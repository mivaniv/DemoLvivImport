services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: lvivimport
      POSTGRES_PASSWORD: lvivimport
      POSTGRES_DB: lvivimport
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lvivimport -d lvivimport"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
